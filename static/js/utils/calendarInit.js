// components/calendarInit.js

import { renderCalendar } from '../components/calendar.js';

export function initCalendarView() {
  // Currently, renderCalendar handles its own setup
  // If you add calendar-specific listeners later, place them here
  console.log("📅 Calendar view initialized.");
}